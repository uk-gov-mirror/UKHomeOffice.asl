apiVersion: v1
kind: Service
metadata:
  name: {{.SERVICE_NAME}}
  labels:
    name: {{.SERVICE_NAME}}
    role: service
spec:
  ports:
  - name: http
    port: 10080
  - name: https
    port: 10443
  selector:
    name: {{.SERVICE_NAME}}
