apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  name: {{.SERVICE_NAME}}-permit-redis-access
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          name: {{.SERVICE_NAME}}
    ports:
    - port: 6379
      protocol: TCP
  podSelector:
    matchLabels:
      name: {{.SERVICE_NAME}}-redis
