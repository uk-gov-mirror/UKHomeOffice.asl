apiVersion: v1
kind: Service
metadata:
  labels:
    name: asl-{{.DEPLOY_TARGET}}-redis
    service: asl-{{.DEPLOY_TARGET}}-redis
  name: asl-{{.DEPLOY_TARGET}}-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    name: asl-{{.DEPLOY_TARGET}}-redis
