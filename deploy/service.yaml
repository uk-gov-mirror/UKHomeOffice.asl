apiVersion: v1
kind: Service
metadata:
  name: asl-dev-app
  labels:
    name: asl-dev-app
    role: service
spec:
  ports:
  - name: http
    port: 80
  - name: https
    port: 443
  selector:
    name: asl-dev-app